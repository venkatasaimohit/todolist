name: todo_list
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  cupertino_icons: ^1.0.8
  flutter_slidable: ^3.0.0 # Fixed missing space

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator : ^1.1.3
  build_runner: ^2.1.11

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
